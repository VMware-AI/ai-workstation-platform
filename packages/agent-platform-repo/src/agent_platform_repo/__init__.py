"""Agent Platform C4 — private PyPI (devpi) skeleton.

Public surface:
    - `cli` : Typer app entry point (`agent-platform-repo`)
    - `auth.load_credentials` : env-driven credential loader (stub for C18)
"""

__version__ = "0.1.0"
