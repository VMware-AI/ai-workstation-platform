[project]
name = "agent-platform-llm-runtime"
version = "0.0.0"
description = "Placeholder — vLLM deployment (C6). See backlog task 6.x."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_platform_llm_runtime"]
