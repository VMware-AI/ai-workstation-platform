[project]
name = "agent-platform-fileshare"
version = "0.0.0"
description = "Placeholder — vSAN File Services SMB integration (C19). See backlog task 19.x."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_platform_fileshare"]
