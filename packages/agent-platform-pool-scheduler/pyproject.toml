[project]
name = "agent-platform-pool-scheduler"
version = "0.0.0"
description = "Placeholder — resource pool + NSX scheduler (C14). See backlog task 14.x."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_platform_pool_scheduler"]
